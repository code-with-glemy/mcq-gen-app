AWS operations

1.Hosting an instance in EC2

Step 1: Login in AWS Mgmt console
Step2. Select create Ec2 instance
Step3: Name the EC2 insrtance
Step4: Select the OS and AMI
Step5: Select the instance type
Step6: Create a key pair
Step7: Select required network settings config
Step8: Lanuch instance 

your instance will be now listed in EC2 instances dashboard



For AWS ubuntu instance
To update the library and packet manager
please execute the below commands
1.sudo apt update
2.sudo apt-get update
3.sudo apt upgrade -y


install git, curl, unzip, tar, make,sudo vim wgte

sudo apt install git curl unzip tar make sudo vim wget -y

git clone <repo link>
sudo apt install python3-pip
python -m venv venv
python install -r requirements.txt
source venv/bin/activate
add .env file
python3 -m streamlit run StreamlitAPP.py
Add inbound rule to the open port for the TCP protocol
